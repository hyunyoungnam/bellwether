# 주간 보고 계획 (수요일 기준)

과제 세부항목 네 가지를 현 프로젝트의 실제 작업으로 매핑한 계획.
각 항목은 목표 산출물 한 줄과 기수행·향후 계획으로 구성.
각 하위 항목의 원래 표현은 유지하고 괄호·부연으로 디테일을 더했으며,
바로 아랫줄(→)은 이 프로젝트에서 그 항목이 정확히 무엇인지의 메모.

---

## 1. MCP 활용 표준화된 도구 연결 기술 개발 (8/19 – 8/26)

**목표 산출물: 학술정보 API 및 분석 모듈의 MCP 기반 표준 스키마 정의 및 통합 호출 인터페이스 개발**

- **기수행** — 학회 논문 수집 및 구조화 추출 파이프라인 구축 (ICML·NeurIPS·ICLR 피드, PMLR·arXiv 원문 대상)
  → `collect→normalize→extract_facts` 체인. 6개 학회-연도 피드 수집 완료, ICML 2개년 9,976편은 vLLM 가이드 디코딩으로 tasks/methods/datasets/한계문장까지 추출 완료.

- **기수행** — 다중 학회·연도 공통 분류 체계 설계 및 동결 (159개 주제 어휘, 학회별 재도출 금지)
  → `config/taxonomy.json`에 어휘를 한 번 확정하고 각 코퍼스에는 적용만 함. 코퍼스별로 따로 뽑으면 연도별 33/45/91개로 갈라져 어휘 크기 차이가 가짜 트렌드가 되는 것을 실측 후 내린 결정.

- **기수행** — 학회 단위 데이터 경로 추상화 및 신규 학회 추가 절차 정립 (`Corpus(venue, year)` 경로 자동 해석)
  → NeurIPS 추가 시 코드 수정 없이 수집 명령만 실행하면 파이프라인 전체가 새 학회를 인식하는 구조 (`papers_neurips_2025.jsonl`식 자동 명명).

- **8/19** — MCP 도구 인터페이스 설계 및 스키마 정의 (검색·트렌드 분석 기능의 입출력 JSON 명세) (예정)
  → 지금 웹 UI가 하는 일(주제·벤치마크·방법·한계문장 검색, 조성 이동 다이제스트)을 에이전트가 호출 가능한 MCP tool 스키마로 옮기는 작업.

- **8/26** — 프로토타입 모듈 코드 작성 및 테스트 (MCP 서버 구현, 에이전트 연동 확인) (예정)
  → 위 스키마대로 동작하는 MCP 서버를 만들고 Claude Code 등 실제 에이전트에서 호출이 도는지 검증.

- **8/26** — 분류 규칙 보강 및 도구 응답 크기 최적화 (상위 n편+집계 요약 형태의 응답 축약 규칙) (예정)
  → 6,637편 목록을 그대로 반환하면 에이전트 컨텍스트가 터짐. 웹 UI의 "요약 먼저, 상세는 클릭" 구조의 API 판.

## 2. 외부 도구 활용 에이전트 추론과정 정합성 확보 기술 개발 (8/19 – 9/30)

**목표 산출물: 연구 트렌드의 다각적 분석을 위한 복수 분석 trajectory 생성 및 통계 검정 기반 결과 교차검증 구조 개발**

- **기수행** — 어휘 기반 검색 경로 구현 (논문 초록 전수의 핵심용어 색인)
  → `build_term_index` — 초록 99.3% 전수에서 변별력 있는 용어만 추려 색인. 제목·추출명만 검색하던 시절 커버리지 20%가 100%로 오른 그 작업.

- **기수행** — 임베딩 기반 유사도 검색 경로 구현 (BGE-M3 1024차원, 브라우저용 PCA128 압축)
  → "같은 뜻인데 다른 단어를 쓴 논문"을 잡는 경로. autonomous driving에서 어휘 경로 59편에 의미 경로가 60편을 더 찾은 사례.

- **기수행** — 분류 체계 기반 탐색 경로 구현 (동결 어휘의 다중 라벨 태그)
  → `topics.json` — 한 논문이 여러 주제를 동시에 가지는 체계. 자율주행 40편이 7개 서브영역에 흩어져 못 찾던 문제의 해법.

- **8/19** — 다중 경로 분석 trajectory 생성 구조 설계 (어휘·임베딩·분류 3개 경로의 병렬 실행) (예정)
  → 하나의 질문("robotics 논문")을 세 경로에 동시에 던지는 실행기. 지금 UI의 "문자 일치 + 의미 확장 별도 표시"가 그 원형.

- **8/26** — 경로 간 일관성 평가 지표 정의 (결과 집합 겹침 기반) (예정)
  → 세 경로가 돌려준 논문 집합의 자카드·포함률을 정합성 점수로. 실측 예: "RL을 연구"와 "RL을 사용"은 겹침 1%로 사실상 다른 집합 — 이런 차이를 수치화.

- **9/02** — 평가 파이프라인 프로토타입 구현 (질의→3경로→지표 산출 자동화) (예정)
  → 질의 목록을 넣으면 경로별 결과와 일치도 표가 자동으로 나오는 배치.

- **9/09** — 질의 집합 구성 및 경로별 결과 수집 (159개 주제 라벨·주요 벤치마크명 기반 표본) (예정)
  → 평가용 대표 질의를 뽑아 전수 실행하고 결과를 쌓는 단계.

- **9/16** — 경로 간 불일치 유형 분류 및 원인 분석 (예정)
  → 어휘만 잡음(표기 차이)·임베딩만 잡음(동의어)·분류만 잡음(하위개념 롤업) 등 불일치가 왜 생기는지 유형화.

- **9/23** — 신뢰 경로 선택 알고리즘 설계 및 적용 (질의 특성별 경로 가중 규칙) (예정)
  → "고유명사 질의는 어휘 경로, 개념 질의는 임베딩 우선" 같은 규칙을 불일치 분석에서 도출.

- **9/30** — 정합성 확보 모듈 통합 및 경로별 결과 비교 제시 (예정)
  → 최종 화면/API에서 "세 경로가 모두 동의한 논문 / 한 경로만 찾은 논문"을 구분 표시.

## 3. 외부 도구 활용 시 환각현상 정량·정성적 분석 (9/30 – 10/28)

**목표 산출물: 대규모 논문 코퍼스 정보 추출에서의 환각 발생률 측정 및 유형·원인 분류 체계 개발**

- **기수행** — 추출 단계 오류 사례 수집 및 발생률 측정 (전 추출 인용문의 원문 전수 대조)
  → `verify_spans()` — "원문 그대로 인용하라"고 시켜도 모델이 지어낸 문장을 전수 대조로 적발. 초기 실측: 17,204개 span 중 11.7%(2,006개)가 원문에 없는 문장.

- **기수행** — 입력 소스별 오류율 차이 관측 (초록 vs 본문, 검증기 오탐과 실제 환각의 분리)
  → 본문 추출의 "환각률 40.7%"를 재분석한 사건. 표본 80건을 사람이 대조하니 실제 환각 0% — 전부 PDF 줄바꿈 하이픈 탓의 검증기 오탐이었고, 수리 후 진짜 환각률은 3.8~4.0%로 확정.

- **9/30** — 환각 유형 taxonomy 설계 (비원문 인용·근거 없는 분류·관계 오류) (예정)
  → 지어낸 인용문 외에 "제안 방법이 있는데 유형은 unknown"(내적 모순 플래그), 역할(proposed/baseline) 오분류까지 묶는 분류표.

- **10/07** — 유형별 발생률 측정 및 정량 분석 (필드별·연구 분야별 교차표) (예정)
  → 위 분류표로 한계문장/방법/데이터셋 필드별, 주제별 발생률 교차표 작성.

- **10/14** — 입력 품질과 환각 발생 간 상관관계 분석 (원문 길이·전처리 잡음 지표 기준) (예정)
  → PDF 파싱 축소율·섹션 분리 성공 여부와 거부율의 상관 — "더러운 입력이 환각을 부르는가".

- **10/21** — 오류 발생 단계 및 전파 경로 추적 (추출–정규화–집계 단계별) (예정)
  → 문자열 "null"이 정규화를 통과해 15편짜리 주제 칩으로 화면까지 올라간 사례처럼, 오류가 어느 단계에서 걸러지고 어디서 새는지 추적.

- **10/28** — 분석 결과 정리 및 탐지 가능성 평가 (예정)
  → 원문 대조 없이도(길이·어휘 중복도 등 신호만으로) 환각을 예측할 수 있는지 평가.

## 4. 논리적 추론 필요 외부 함수 활용 정합성 확보 전략 개발 (10/28 – 11/25)

**목표 산출물: 원문 대조 기반 추출 결과 자동 검증기 및 검증 실패 항목의 선택적 재처리 전략 개발**

- **기수행** — 원문 대조 기반 추출 결과 검증 체계 구현 (부분문자열 검증, 조판 잡음 정규화 포함)
  → 모든 추출 인용문은 원문의 부분문자열임을 확인해야만 표시. `condition-␣ing`→`conditioning` 류의 조판 잡음은 정규화하되, 부분수열 완화는 의도적으로 거부 — 그러면 아무 문장이나 통과해 검증이 무의미해짐.

- **기수행** — 검증 실패 항목의 자동 배제 절차 적용
  → 실패는 수리하지 않고 버림 — "근사 일치는 다시 쓴 것이고, 다시 쓴 것은 증거가 아니다."

- **10/28** — 검증기 구성 요소 설계 (트렌드 주장의 근거 데이터 재계산 대조 구조) (예정)
  → 다이제스트의 각 주장("image generation에서 diffusion 48→25%")을 근거 논문 집합에서 재계산해 일치를 확인하는 구조 설계.

- **11/04** — Trend Analysis Verifier 프로토타입 구현 (예정)
  → 화면·API가 내보내는 모든 수치 문장에 재계산 검증을 물리는 프로토타입.

- **11/11** — 근거 충분성 판정 규칙 추가 및 테스트 (표본 크기·통계 검정 통과 기준) (예정)
  → 지금 UI에 산재한 문턱들(양쪽 연도 12편 미만이면 비교 안 함, z≥2.576+실질성, 0→8편 등장 규칙)을 하나의 판정 모듈로 명문화.

- **11/18** — Selective repair 전략 구현 및 비용 비교 (예정)
  → 검증에서 떨어진 필드만 골라 재프롬프트하는 전략. 전량 재추출(GPU 약 6시간) 대비 비용·회복률 비교.

- **11/25** — 검증 전후 오류율 비교 및 전략 유효성 평가 (예정)
  → 거부율(11.7%→4%대)·표시 커버리지(96→99%) 같은 지표로 전략의 효과를 정량 평가.

---

## 외부 피드백 (2026-09-01) — Shifts 차트 가독성 (보류, 고민 필요)

피드백: "이 시각화가 무엇을 의미하는지 한번에 와닿지 않는다."
현 상태: 행마다 학회 3색 × [연한=작년, 진한=올해] 쌍 레인, 로그축, 단위는
그 학회 논문의 1,000편당 비중. 범례는 학회 카드 아래에 있어 차트에서 멀다.
검토했던 처방(미적용): ① 범례를 차트 헤더 안으로 ② 단위 캡션 한 줄
③ 첫 행에만 연도 미니 라벨 ④ 기본은 풀링 한 줄 + 펼치면 학회별 레인.
원칙 충돌 지점: "설명이 필요한 뷰는 다시 설계하라" — 캡션 추가로 때우는 것은
원칙 위반에 가깝고, 인코딩 자체의 단순화가 정답일 수 있음.
