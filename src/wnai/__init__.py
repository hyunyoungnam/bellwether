"""Local-first platform launcher: one command serves the site + search on the LAN.

Provisional package name — the repo rename is pending; only pyproject.toml's
[project.scripts] line and this directory name change when it lands.
"""
